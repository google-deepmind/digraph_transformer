# @package _global_

config:
  task: dista
  data: ./data/distance
  wandb:
    project: digt-playground
    settings:
      code_dir: .

overwrite:
  experiment_kwargs:
    config:
      model:
        gnn_config:
          se: ~
