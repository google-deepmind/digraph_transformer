# @package _global_

config:
  task: sn
  data: ./data/sorting_network
  wandb:
    project: digt-sn
    settings:
      code_dir: .

overwrite:
  experiment_kwargs:
    config:
      model:
        gnn_config:
          se: ~
