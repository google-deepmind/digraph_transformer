# @package _global_
default:
  - override 

config:
  presets: bignn,bbs
  wandb:
    tags:
      - vanilla

overwrite:
  experiment_kwargs:
    config:
      model:
        posenc_config:
          exclude_canonical: True
