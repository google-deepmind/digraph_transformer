# @package _global_
default:
  - override 

config:
  presets: bignn,rwpos,bbs
  wandb:
    tags: 
      - rw

overwrite:
  experiment_kwargs:
    config:
      model:
        posenc_config:
          relative_positional_encodings: True
