# @package _global_
default:
  - override 

config:
  presets: bignn,maglappos,bbs
  wandb:
    tags: 
      - svd

overwrite:
  experiment_kwargs:
    config:
      model:
        posenc_config:
          posenc_type: svd
