# @package _global_

config:
  presets: bignn,maglappos,bbs
  wandb:
    tags: 
      - maglap_inv

overwrite:
  experiment_kwargs:
    config:
      model:
        posenc_config:
          maglap_net_type: invariant

# hydra:
#   sweeper:
#     params:
#       +overwrite.experiment_kwargs.config.model.model_type: sat
